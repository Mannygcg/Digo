import SwiftUI

struct SettingsPlaceholderView: View {
    var body: some View {
        Text("Digo settings coming in Phase 4.")
            .padding()
            .frame(width: 320, height: 120)
    }
}
